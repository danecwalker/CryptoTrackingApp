import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

interface DetailViewProps {
  
};

const DetailView = ({} : DetailViewProps) => {
  return (
    <View>
        <Text>Hello DetailView</Text>
    </View>
  );
};

export default DetailView;